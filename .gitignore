# heavy/local data
datasets/raw/
datasets/processed/
models/
tasks/
venvs/

# external cloned repos can be huge; keep manifests/notes/scripts in this repo
repos/

# caches/build artifacts
__pycache__/
*.pyc
.cache/
.next/
node_modules/
*.log
*.tmp
.DS_Store

# local browser and agent diagnostics (reproducible; never product assets)
.playwright-mcp/
.agents/shared-session/time/
atlas-*.png

# graphify caches and dated run snapshots; keep graph.json/report tracked
graphify-out/cache/
graphify-out/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
