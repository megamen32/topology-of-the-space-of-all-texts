let model, wordModel, sentenceModel;
function generateFSM(seed,length){const rng=mulberry32(hashSeed(seed));let out='',prev='START',last='',rep=0,spaceRun=0,emojiRun=0;for(let i=0;i<length;i++){let trans=model.transitions[prev]||model.transitions.START;let nextCls=weightedChoice(trans,rng,0.58,([k])=>!(prev==='SPACE'&&k==='SPACE')&&!(emojiRun>3&&k==='EMOJI'))||'SPACE';let em=model.emissions[nextCls]||{' ':1};let ch=weightedChoice(em,rng,0.70,([k])=>!(rep>3&&k===last))||' ';if(ch===' '&&spaceRun>0){nextCls=rng()<0.55?'RU':'EN';em=model.emissions[nextCls]||em;ch=weightedChoice(em,rng,0.78)||' ';}out+=ch;rep=ch===last?rep+1:0;last=ch;spaceRun=ch===' '?spaceRun+1:0;emojiRun=nextCls==='EMOJI'?emojiRun+1:0;prev=nextCls;}return out;}
function realizeAbstract(tok,rng){const list=(wordModel.abstract_emissions&&wordModel.abstract_emissions[tok])||null;if(!list||!list.length)return tok.replace(/[<>]/g,'');return list[Math.floor(rng()*list.length)]||'';}
function isPunct(tok){return /^[.,!?;:)]$/.test(tok)}
function detok(tokens){let s='';for(const tok of tokens){if(!tok||tok==='</s>')continue;if(!s)s=tok;else if(isPunct(tok))s+=tok;else s+=' '+tok;}return s;}
function generateSentenceFromTemplate(template,rng){const types=template.split(/\s+/).filter(Boolean);let prev='<s>',toks=[];for(const type of types){let trans=wordModel.transitions[prev]||wordModel.transitions['<s>']||{};let tok=weightedChoice(trans,rng,0.62,([k])=>{if(k==='</s>')return type==='T';if(type==='R')return k==='<ru>'||/^[а-яё]+$/.test(k);if(type==='L')return k==='<en>'||/^[a-z]+$/.test(k);if(type==='E')return k==='<emoji>'||/\p{Emoji}/u.test(k);if(type==='N')return k==='<num>'||/^[0-9]+$/.test(k);if(type==='T')return ['.','!','?','</s>'].includes(k);if(type==='P')return [',',';',':'].includes(k);return true;});if(!tok||tok==='</s>')tok=type==='T'?(rng()<0.55?'.':rng()<0.8?'!':'?'):'<ru>';tok=realizeAbstract(tok,rng);toks.push(tok);prev=tok;}let s=detok(toks).replace(/\s+([.,!?;:])/g,'$1');if(!/[.!?…]$/.test(s))s+=rng()<0.55?'.':rng()<0.8?'!':'?';return s;}
function generateSentenceStudent(seed,length){const rng=mulberry32(hashSeed(seed));const templates=sentenceModel.templates||[];let out=[],chars=0;while(chars<length){const row=weightedArray(templates,rng,0.72);const tpl=row?row.template:'R R R T';let s=generateSentenceFromTemplate(tpl,rng);out.push(s);chars+=s.length+1;if(out.length%(2+Math.floor(rng()*4))===0){out.push('\n');chars+=1;}if(out.length>200)break;}return out.join(' ').replace(/\n\s+/g,'\n').slice(0,length);}
async function boot(){model=await loadCore();wordModel=await fetch('data/word_student.json?ts='+Date.now()).then(r=>r.json());sentenceModel=await fetch('data/sentence_student.json?ts='+Date.now()).then(r=>r.json());document.getElementById('generate').onclick=()=>{const seed=document.getElementById('seed').value;const len=Math.max(32,Math.min(4096,Number(document.getElementById('len').value)||512));const mode=document.getElementById('mode').value;document.getElementById('page').value=mode==='fsm'?generateFSM(seed,len):generateSentenceStudent(seed,len);};document.getElementById('scoreGenerated').onclick=()=>{document.getElementById('result').textContent=show(scoreText(model,document.getElementById('page').value));};document.getElementById('generate').click();}
boot();
